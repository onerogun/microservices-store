server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://discUser:discPassword@discovery-server:8761/eureka/
      #defaultZone: http://discUser:discPassword@localhost:8761/eureka/

spring:
  redis:
    host: redis
    port: 6379